# Elements Baseball v1.3.8 — Team Builder UI Consistency Pass

- Preserves the approved v1.3.7 Cards page and Player Profile layouts.
- Keeps Team Builder's player-selection drawer as a purpose-built overlay rather than cloning the Cards page.
- Updates the drawer's visual hierarchy to match Cards: Quick Sort, Sort By, Filters, Attribute Filters, Applied Filters.
- Renames the drawer presentation label `Attribute Sort` to `Sort By` without changing sorting behavior.
- Renames `Minimum DEF` to `DEF` without changing defense filtering behavior.
- Adds compact removable Applied Filters chips inside the drawer for active search/filter restrictions.
- Keeps Season Eligible in the Team Builder header; its active state is echoed in Applied Filters and can be removed there by switching the existing toggle off.
- Does not add Chart Type to Team Builder.
- Standardizes gold active states, compact control sizing, labels, borders, and responsive organization with the Cards visual language.
- Preserves all existing roster slot, card-selection, sorting, filtering, drag/reorder, and saved-lineup behavior.
